<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">

  <title>FlowCRM | Calculadora de ROI para Automação de Marketing</title>

  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Manrope:wght@400;500;600;700;800&display=swap" rel="stylesheet">

  <script>
    document.documentElement.classList.add("js");
  </script>

  <style>
    :root {
      --pink: #ff4f8b;
      --orange: #ff7a30;
      --ink: #191923;
      --ink-soft: #4f4f5f;
      --white: #ffffff;
      --dark: #111116;
      --gradient: linear-gradient(135deg, var(--pink) 0%, var(--orange) 100%);
      --container: 1180px;
      --radius-lg: 42px;
    }

    * {
      box-sizing: border-box;
    }

    html {
      scroll-behavior: smooth;
    }

    body {
      margin: 0;
      font-family: "Manrope", Arial, sans-serif;
      background: var(--white);
      color: var(--ink);
      overflow-x: hidden;
    }

    img {
      display: block;
      max-width: 100%;
    }

    a {
      color: inherit;
      text-decoration: none;
    }

    .gradient-text {
      background: var(--gradient);
      -webkit-background-clip: text;
      background-clip: text;
      color: transparent;
    }

    .section-label {
      display: inline-flex;
      align-items: center;
      gap: 11px;
      margin-bottom: 24px;
      color: var(--pink);
      font-size: .75rem;
      font-weight: 800;
      letter-spacing: .14em;
      text-transform: uppercase;
    }

    .section-label::before {
      content: "";
      width: 28px;
      height: 2px;
      border-radius: 999px;
      background: var(--gradient);
    }

    /* Conteúdo visível por padrão.
       Só escondemos para animar quando JS está ativo. */
    .reveal {
      opacity: 1;
      transform: none;
    }

    .js .reveal {
      opacity: 0;
      transform: translateY(24px);
      transition:
        opacity .75s ease,
        transform .75s cubic-bezier(.2,.8,.2,1);
    }

    .js .reveal.is-visible {
      opacity: 1;
      transform: translateY(0);
    }

    .delay-1 { transition-delay: .08s !important; }
    .delay-2 { transition-delay: .16s !important; }
    .delay-3 { transition-delay: .24s !important; }


    /* ==================================================
       NAV
    ================================================== */

    .site-header {
      position: absolute;
      top: 22px;
      left: 0;
      width: 100%;
      z-index: 20;
    }

    .site-nav {
      width: min(1180px, calc(100% - 48px));
      min-height: 66px;
      margin: 0 auto;
      padding: 10px 12px 10px 20px;
      display: flex;
      align-items: center;
      justify-content: space-between;
      gap: 24px;
      border: 1px solid rgba(255,255,255,.72);
      border-radius: 22px;
      background: rgba(255,255,255,.72);
      box-shadow: 0 16px 50px rgba(25,25,35,.07);
      backdrop-filter: blur(18px);
    }

    .logo {
      display: flex;
      align-items: center;
    }

    .logo img {
      width: 142px;
    }


    /* ==================================================
       BUTTONS
    ================================================== */

    .button {
      display: inline-flex;
      align-items: center;
      justify-content: center;
      gap: 10px;
      min-height: 54px;
      padding: 17px 26px;
      border-radius: 999px;
      background: var(--gradient);
      color: #fff;
      font-size: .93rem;
      font-weight: 800;
      box-shadow: 0 16px 38px rgba(255,79,139,.21);
      transition:
        transform .25s ease,
        box-shadow .25s ease;
    }

    .button svg {
      width: 18px;
      height: 18px;
      transition: transform .25s ease;
    }

    .button:hover {
      transform: translateY(-3px);
      box-shadow: 0 24px 54px rgba(255,79,139,.31);
    }

    .button:hover svg {
      transform: translateX(4px);
    }

    .nav-button {
      min-height: 44px;
      padding: 13px 19px;
      font-size: .82rem;
      box-shadow: 0 10px 28px rgba(255,79,139,.18);
    }


    /* ==================================================
       HERO
    ================================================== */

    .hero-section {
      position: relative;
      min-height: 94svh;
      padding: 154px 24px 82px;
      display: flex;
      align-items: center;
      background:
        radial-gradient(circle at 95% 6%, rgba(255,79,139,.16), transparent 32%),
        radial-gradient(circle at 4% 82%, rgba(255,122,48,.08), transparent 28%),
        linear-gradient(180deg, #fff7f3 0%, #ffffff 92%);
      overflow: hidden;
    }

    .hero-section::before {
      content: "";
      position: absolute;
      width: 650px;
      height: 650px;
      top: -340px;
      right: -170px;
      border-radius: 50%;
      background: radial-gradient(circle, rgba(255,79,139,.18), transparent 68%);
      animation: heroOrb 10s ease-in-out infinite alternate;
      pointer-events: none;
    }

    @keyframes heroOrb {
      to {
        transform: translate3d(-30px, 38px, 0) scale(1.07);
      }
    }

    .hero-container {
      position: relative;
      z-index: 2;
      width: 100%;
      max-width: var(--container);
      margin: 0 auto;
      display: grid;
      grid-template-columns: minmax(0,1.06fr) minmax(390px,.94fr);
      gap: clamp(54px, 7vw, 90px);
      align-items: center;
    }

    .hero-copy {
      max-width: 675px;
    }

    .hero-eyebrow {
      display: inline-flex;
      align-items: center;
      gap: 9px;
      padding: 9px 13px;
      margin-bottom: 23px;
      border: 1px solid rgba(255,79,139,.13);
      border-radius: 999px;
      background: rgba(255,255,255,.7);
      box-shadow: 0 10px 28px rgba(25,25,35,.045);
      color: #666675;
      font-size: .77rem;
      font-weight: 700;
    }

    .hero-eyebrow::before {
      content: "";
      width: 7px;
      height: 7px;
      border-radius: 50%;
      background: var(--pink);
      box-shadow: 0 0 0 5px rgba(255,79,139,.09);
    }

    .hero-copy h1 {
      margin: 0;
      max-width: 700px;
      font-size: clamp(3.35rem, 5.7vw, 5.6rem);
      line-height: .95;
      letter-spacing: -.074em;
    }

    .hero-description {
      margin: 26px 0 0;
      max-width: 570px;
      color: var(--ink-soft);
      font-size: clamp(1.02rem, 1.55vw, 1.14rem);
      line-height: 1.7;
    }

    .hero-actions {
      margin-top: 32px;
      display: flex;
      align-items: center;
      gap: 18px;
      flex-wrap: wrap;
    }

    .hero-visual {
      position: relative;
      perspective: 1200px;
    }

    .mockup-stage {
      position: relative;
      padding: 10px;
      border: 1px solid rgba(255,255,255,.9);
      border-radius: 30px;
      background: rgba(255,255,255,.62);
      box-shadow: 0 38px 92px rgba(25,25,35,.15);
      backdrop-filter: blur(20px);
      transform: perspective(1200px) rotateY(-5deg) rotateX(2deg);
      transition: transform .5s cubic-bezier(.2,.8,.2,1);
    }

    .hero-visual:hover .mockup-stage {
      transform: perspective(1200px) rotateY(-1deg) rotateX(0deg) translateY(-6px);
    }

    .mockup-stage img {
      width: 100%;
      border-radius: 22px;
    }

    .mockup-stage::before {
      content: "";
      position: absolute;
      inset: -36px;
      z-index: -1;
      border-radius: 50px;
      background: linear-gradient(135deg, rgba(255,79,139,.17), rgba(255,122,48,.09));
      filter: blur(34px);
      opacity: .72;
    }

    .floating-note {
      position: absolute;
      left: -34px;
      bottom: -28px;
      max-width: 205px;
      padding: 15px 17px;
      border: 1px solid rgba(255,255,255,.84);
      border-radius: 18px;
      background: rgba(255,255,255,.86);
      box-shadow: 0 20px 55px rgba(25,25,35,.11);
      backdrop-filter: blur(16px);
      color: #575765;
      font-size: .78rem;
      line-height: 1.45;
      animation: floatNote 4.5s ease-in-out infinite alternate;
    }

    .floating-note strong {
      display: block;
      margin-bottom: 3px;
      color: var(--ink);
      font-size: .82rem;
    }

    @keyframes floatNote {
      to { transform: translateY(-8px); }
    }


    /* ==================================================
       CHALLENGE
    ================================================== */

    .challenge-section {
      position: relative;
      padding: clamp(78px, 8vw, 104px) 24px clamp(58px, 6vw, 78px);
      background:
        radial-gradient(circle at 8% 22%, rgba(255,79,139,.05), transparent 24%),
        radial-gradient(circle at 92% 70%, rgba(255,122,48,.035), transparent 25%),
        #ffffff;
      overflow: hidden;
    }

    .challenge-number {
      position: absolute;
      top: 12px;
      right: max(24px, calc((100vw - 1180px) / 2));
      color: rgba(25,25,35,.026);
      font-size: clamp(8rem, 17vw, 16rem);
      font-weight: 800;
      line-height: 1;
      letter-spacing: -.08em;
      pointer-events: none;
    }

    .challenge-container {
      position: relative;
      z-index: 2;
      max-width: var(--container);
      margin: 0 auto;
      display: grid;
      grid-template-columns: minmax(0,1.08fr) minmax(390px,.92fr);
      gap: clamp(62px, 8vw, 108px);
      align-items: center;
    }

    .challenge-heading h2 {
      margin: 0;
      max-width: 700px;
      font-size: clamp(3rem, 4.8vw, 4.85rem);
      line-height: .99;
      letter-spacing: -.065em;
    }

    .challenge-content {
      position: relative;
    }

    .challenge-content::before {
      content: "";
      position: absolute;
      top: 0;
      bottom: 0;
      left: -31px;
      width: 1px;
      background: linear-gradient(180deg, var(--pink), rgba(255,122,48,.1));
    }

    .challenge-intro {
      margin: 0;
      color: #555563;
      font-size: 1.06rem;
      line-height: 1.78;
    }

    .challenge-conclusion {
      margin: 28px 0 0;
      padding-top: 25px;
      border-top: 1px solid rgba(25,25,35,.08);
      color: var(--ink);
      font-size: 1.02rem;
      font-weight: 700;
      line-height: 1.62;
    }


    /* ==================================================
       BEFORE / AFTER IMAGE
    ================================================== */

    .challenge-visual-wrap {
      position: relative;
      z-index: 3;
      width: min(1240px, calc(100% - 48px));
      margin: clamp(44px, 5vw, 64px) auto 0;
    }

    .challenge-visual-wrap::before {
      content: "";
      position: absolute;
      inset: 10% 6% -5%;
      border-radius: 50px;
      background: linear-gradient(120deg, rgba(255,79,139,.11), rgba(255,122,48,.08));
      filter: blur(42px);
      opacity: .72;
      pointer-events: none;
    }

    .challenge-visual {
      position: relative;
      padding: 9px;
      border: 1px solid rgba(25,25,35,.07);
      border-radius: 32px;
      background: rgba(255,255,255,.86);
      box-shadow: 0 32px 82px rgba(25,25,35,.12);
      overflow: hidden;
      transition:
        transform .45s cubic-bezier(.2,.8,.2,1),
        box-shadow .45s ease;
    }

    .challenge-visual:hover {
      transform: translateY(-5px);
      box-shadow: 0 44px 96px rgba(25,25,35,.16);
    }

    .challenge-visual img {
      width: 100%;
      height: auto;
      border-radius: 24px;
    }


    /* ==================================================
       MOVING BACKGROUND TRANSITION
    ================================================== */

    .section-transition {
      position: relative;
      height: 76px;
      margin-top: -1px;
      background:
        linear-gradient(
          180deg,
          #ffffff 0%,
          #fbf8f8 46%,
          #f7f7f8 100%
        );
      overflow: hidden;
      isolation: isolate;
    }

    .section-transition::before {
      content: "";
      position: absolute;
      z-index: 1;
      top: -60px;
      left: -5%;
      width: 110%;
      height: 92px;
      border-radius: 0 0 50% 50%;
      background: #ffffff;
      transform: rotate(-1.1deg);
      transform-origin: center;
    }

    .section-transition::after {
      content: "";
      position: absolute;
      z-index: 0;
      width: 58%;
      height: 100px;
      left: 18%;
      bottom: -63px;
      border-radius: 50%;
      background:
        linear-gradient(
          90deg,
          rgba(255,79,139,.15),
          rgba(255,122,48,.11)
        );
      filter: blur(26px);
      animation: transitionDrift 8s ease-in-out infinite alternate;
    }

    @keyframes transitionDrift {
      from {
        transform: translateX(-8%) scaleX(.92);
        opacity: .55;
      }
      to {
        transform: translateX(12%) scaleX(1.12);
        opacity: .85;
      }
    }


    /* ==================================================
       CALCULATOR
    ================================================== */

    .calculator-section {
      width: 100%;
      padding: 0 24px 96px;
      background:
        radial-gradient(circle at 82% 0%, rgba(255,79,139,.055), transparent 24%),
        linear-gradient(180deg, #f7f7f8 0%, #ffffff 72%);
    }

    .calculator-shell {
      --mx: 76%;
      --my: 18%;
      position: relative;
      max-width: 1280px;
      margin: 0 auto;
      padding: clamp(64px, 7vw, 92px);
      display: grid;
      grid-template-columns: minmax(0,.82fr) minmax(480px,1.18fr);
      gap: clamp(58px, 7vw, 96px);
      align-items: center;
      border-radius: var(--radius-lg);
      background:
        radial-gradient(circle at var(--mx) var(--my), rgba(255,79,139,.18), transparent 28%),
        radial-gradient(circle at 72% 88%, rgba(255,122,48,.11), transparent 28%),
        linear-gradient(145deg, #1a1a21, #101015 58%, #17171c);
      box-shadow: 0 44px 98px rgba(25,25,35,.15);
      overflow: hidden;
    }

    .calculator-shell::before {
      content: "";
      position: absolute;
      width: 420px;
      height: 420px;
      top: -260px;
      right: -110px;
      border-radius: 50%;
      background: radial-gradient(circle, rgba(255,79,139,.14), transparent 68%);
      pointer-events: none;
    }

    .calculator-shell::after {
      content: "";
      position: absolute;
      width: 380px;
      height: 380px;
      left: -230px;
      bottom: -245px;
      border-radius: 50%;
      background: radial-gradient(circle, rgba(255,122,48,.08), transparent 70%);
      pointer-events: none;
    }

    .calculator-copy {
      position: relative;
      z-index: 2;
    }

    .calculator-shell .section-label {
      color: #ff82a7;
    }

    .calculator-copy h2 {
      margin: 0;
      max-width: 550px;
      color: #fff;
      font-size: clamp(2.9rem, 4.4vw, 4.55rem);
      line-height: .99;
      letter-spacing: -.065em;
    }

    .calculator-description {
      margin: 27px 0 0;
      max-width: 510px;
      color: rgba(255,255,255,.64);
      font-size: 1.02rem;
      line-height: 1.73;
    }

    .calculator-cta {
      margin-top: 32px;
    }


    /* ==================================================
       IMPACT CONTENT
    ================================================== */

    .impact-visual {
      position: relative;
      z-index: 2;
    }

    .impact-eyebrow {
      margin-bottom: 20px;
      display: flex;
      align-items: center;
      justify-content: space-between;
      gap: 22px;
      color: rgba(255,255,255,.4);
      font-size: .7rem;
      font-weight: 800;
      letter-spacing: .055em;
      text-transform: uppercase;
    }

    .status {
      display: inline-flex;
      align-items: center;
      gap: 8px;
      flex: 0 0 auto;
      color: rgba(255,255,255,.45);
      font-size: .7rem;
      font-weight: 600;
      letter-spacing: 0;
      text-transform: none;
    }

    .status::before {
      content: "";
      width: 7px;
      height: 7px;
      border-radius: 50%;
      background: var(--pink);
      box-shadow: 0 0 0 5px rgba(255,79,139,.09);
    }

    .impact-list {
      position: relative;
      display: flex;
      flex-direction: column;
      gap: 12px;
    }

    .impact-list::before {
      content: "";
      position: absolute;
      top: 34px;
      bottom: 34px;
      left: 25px;
      width: 1px;
      background: linear-gradient(var(--pink), var(--orange));
      opacity: .42;
    }

    .impact-card {
      position: relative;
      min-height: 122px;
      padding: 21px 24px 21px 76px;
      border: 1px solid rgba(255,255,255,.085);
      border-radius: 20px;
      background:
        linear-gradient(
          135deg,
          rgba(255,255,255,.082),
          rgba(255,255,255,.035)
        );
      box-shadow:
        inset 0 1px 0 rgba(255,255,255,.045),
        0 18px 36px rgba(0,0,0,.1);
      backdrop-filter: blur(16px);
      overflow: hidden;
      transition:
        transform .3s ease,
        border-color .3s ease,
        background .3s ease;
    }

    .impact-card:nth-child(1) {
      margin-right: 30px;
    }

    .impact-card:nth-child(2) {
      margin-left: 18px;
      margin-right: 8px;
    }

    .impact-card:nth-child(3) {
      margin-left: 6px;
      margin-right: 24px;
    }

    .impact-card:nth-child(4) {
      margin-left: 24px;
    }

    .impact-card::after {
      content: "";
      position: absolute;
      width: 170px;
      height: 170px;
      right: -100px;
      top: -110px;
      border-radius: 50%;
      background: radial-gradient(circle, rgba(255,79,139,.15), transparent 68%);
      opacity: 0;
      transition: opacity .3s ease;
      pointer-events: none;
    }

    .impact-card:hover {
      transform: translateX(7px);
      border-color: rgba(255,112,157,.22);
      background:
        linear-gradient(
          135deg,
          rgba(255,255,255,.11),
          rgba(255,255,255,.045)
        );
    }

    .impact-card:hover::after {
      opacity: 1;
    }

    .impact-number {
      position: absolute;
      top: 21px;
      left: 18px;
      width: 38px;
      height: 38px;
      display: flex;
      align-items: center;
      justify-content: center;
      border: 1px solid rgba(255,100,145,.18);
      border-radius: 50%;
      background: rgba(255,79,139,.12);
      color: #ff94b3;
      font-size: .66rem;
      font-weight: 800;
      z-index: 2;
    }

    .impact-tag {
      display: block;
      margin-bottom: 6px;
      color: #ff84a7;
      font-size: .64rem;
      font-weight: 800;
      letter-spacing: .11em;
      text-transform: uppercase;
    }

    .impact-card h3 {
      margin: 0;
      color: #fff;
      font-size: 1.04rem;
      line-height: 1.3;
      letter-spacing: -.024em;
    }

    .impact-card p {
      margin: 6px 0 0;
      max-width: 435px;
      color: rgba(255,255,255,.51);
      font-size: .84rem;
      line-height: 1.5;
    }


    /* ==================================================
       TABLET
    ================================================== */

    @media (max-width: 1000px) {
      .hero-section {
        min-height: auto;
        padding-top: 142px;
      }

      .hero-container,
      .challenge-container,
      .calculator-shell {
        grid-template-columns: 1fr;
      }

      .hero-copy {
        max-width: 760px;
      }

      .hero-visual {
        max-width: 650px;
        margin: 14px auto 0;
      }

      .challenge-content {
        max-width: 700px;
      }

      .challenge-content::before {
        display: none;
      }

      .calculator-copy {
        max-width: 720px;
      }

      .impact-visual {
        max-width: 720px;
      }
    }


    /* ==================================================
       MOBILE
    ================================================== */

    @media (max-width: 680px) {
      .site-header {
        top: 14px;
      }

      .site-nav {
        width: calc(100% - 28px);
        min-height: 58px;
        padding: 8px 9px 8px 15px;
        border-radius: 18px;
      }

      .logo img {
        width: 118px;
      }

      .nav-button {
        padding: 11px 14px;
        font-size: .74rem;
      }

      .nav-button svg {
        display: none;
      }

      .hero-section {
        padding: 126px 20px 68px;
      }

      .hero-copy h1 {
        font-size: clamp(2.8rem, 13vw, 4rem);
      }

      .hero-description {
        font-size: 1rem;
        line-height: 1.65;
      }

      .hero-actions {
        align-items: flex-start;
        flex-direction: column;
      }

      .button {
        width: 100%;
      }

      .mockup-stage {
        transform: none;
        border-radius: 23px;
      }

      .mockup-stage img {
        border-radius: 17px;
      }

      .floating-note {
        position: relative;
        left: auto;
        bottom: auto;
        margin-top: 12px;
        max-width: none;
        animation: none;
      }

      .challenge-section {
        padding: 72px 20px 54px;
      }

      .challenge-number {
        top: 16px;
        right: 18px;
      }

      .challenge-container {
        gap: 38px;
      }

      .challenge-heading h2 {
        font-size: clamp(2.6rem, 12vw, 3.55rem);
      }

      .challenge-intro {
        font-size: 1rem;
      }

      .challenge-visual-wrap {
        width: calc(100% - 20px);
        margin-top: 38px;
      }

      .challenge-visual {
        padding: 6px;
        border-radius: 22px;
      }

      .challenge-visual img {
        border-radius: 17px;
      }

      .section-transition {
        height: 58px;
      }

      .calculator-section {
        padding: 0 14px 78px;
      }

      .calculator-shell {
        padding: 58px 21px;
        gap: 46px;
        border-radius: 28px;
      }

      .calculator-copy h2 {
        font-size: clamp(2.5rem, 11vw, 3.45rem);
      }

      .calculator-description {
        font-size: 1rem;
      }

      .impact-eyebrow {
        align-items: flex-start;
        flex-direction: column;
        line-height: 1.45;
      }

      .status {
        display: none;
      }

      .impact-list::before {
        display: none;
      }

      .impact-card,
      .impact-card:nth-child(1),
      .impact-card:nth-child(2),
      .impact-card:nth-child(3),
      .impact-card:nth-child(4) {
        margin: 0;
        min-height: auto;
        padding: 68px 20px 20px;
      }

      .impact-number {
        top: 18px;
        left: 18px;
      }

      .impact-card:hover {
        transform: translateY(-2px);
      }
    }


    /* ==================================================
       REDUCED MOTION
    ================================================== */

    @media (prefers-reduced-motion: reduce) {
      *,
      *::before,
      *::after {
        animation: none !important;
        transition: none !important;
      }

      .js .reveal {
        opacity: 1;
        transform: none;
      }
    }
  </style>
</head>

<body>

  <header class="site-header">
    <nav class="site-nav">
      <a href="#" class="logo" aria-label="FlowCRM">
        <img
          src="https://res.cloudinary.com/wk2nvqhl/image/upload/f_auto,q_auto/logo_oficial_k6kpzn"
          alt="FlowCRM"
        >
      </a>

      <a href="#calculator" class="button nav-button">
        Acessar calculadora
        <svg viewBox="0 0 24 24" fill="none" aria-hidden="true">
          <path
            d="M5 12h14m-6-6 6 6-6 6"
            stroke="currentColor"
            stroke-width="2"
            stroke-linecap="round"
            stroke-linejoin="round"
          />
        </svg>
      </a>
    </nav>
  </header>


  <main>

    <!-- HERO -->
    <section class="hero-section">
      <div class="hero-container">

        <div class="hero-copy">
          <span class="hero-eyebrow reveal">
            Marketing Automation ROI Calculator
          </span>

          <h1 class="reveal delay-1">
            Calcule o ROI da automação de marketing em
            <span class="gradient-text">menos de 2 minutos</span>
          </h1>

          <p class="hero-description reveal delay-2">
            Descubra quanto tempo sua equipe pode economizar ao
            automatizar tarefas manuais, melhorar fluxos de campanha
            e reduzir esforço operacional.
          </p>

          <div class="hero-actions reveal delay-3">
            <a href="#calculator" class="button">
              Ver minha estimativa de economia
              <svg viewBox="0 0 24 24" fill="none" aria-hidden="true">
                <path
                  d="M5 12h14m-6-6 6 6-6 6"
                  stroke="currentColor"
                  stroke-width="2"
                  stroke-linecap="round"
                  stroke-linejoin="round"
                />
              </svg>
            </a>
          </div>
        </div>

        <div class="hero-visual reveal delay-2">
          <div class="mockup-stage">
            <img
              src="https://res.cloudinary.com/wk2nvqhl/image/upload/v1783472860/Mockup_de_painel_de_marketing_FlowCRM_nouecr.png"
              alt="Mockup da calculadora de ROI da FlowCRM"
            >
          </div>

          <div class="floating-note">
            <strong>Estimativa personalizada</strong>
            Baseada na rotina atual da sua equipe.
          </div>
        </div>

      </div>
    </section>


    <!-- O DESAFIO -->
    <section class="challenge-section">

      <span class="challenge-number" aria-hidden="true">01</span>

      <div class="challenge-container">

        <div class="challenge-heading reveal">
          <span class="section-label">O desafio</span>

          <h2>
            Quanto tempo sua equipe ainda perde com
            <span class="gradient-text">tarefas manuais?</span>
          </h2>
        </div>

        <div class="challenge-content reveal delay-1">
          <p class="challenge-intro">
            Campanhas, listas, aprovações, relatórios e follow-ups
            fazem parte da rotina de marketing. Quando essas tarefas
            começam a consumir tempo demais, sobra menos espaço para
            análise, planejamento e melhoria das campanhas.
          </p>

          <p class="challenge-conclusion">
            Automatizar processos pode reduzir retrabalho e liberar
            a equipe para atividades de maior impacto.
          </p>
        </div>

      </div>

      <div class="challenge-visual-wrap reveal delay-1">
        <div class="challenge-visual">
          <img
            src="https://res.cloudinary.com/wk2nvqhl/image/upload/v1787862097/ChatGPT_Image_21_de_jul._de_2026_20_42_39_eqn2rv.png"
            alt="Comparação visual de um escritório antes e depois da automação"
          >
        </div>
      </div>

    </section>


    <!-- TRANSIÇÃO -->
    <div class="section-transition" aria-hidden="true"></div>


    <!-- A CALCULADORA -->
    <section class="calculator-section" id="calculator">

      <div class="calculator-shell">

        <div class="calculator-copy reveal">
          <span class="section-label">A calculadora</span>

          <h2>
            Descubra onde a automação
            <span class="gradient-text">pode gerar mais impacto</span>
          </h2>

          <p class="calculator-description">
            Com base na rotina atual da sua equipe, a calculadora
            estima o potencial de redução de esforço manual e
            economia de tempo.
          </p>

          <a href="#" class="button calculator-cta">
            Calcular economia da minha equipe

            <svg viewBox="0 0 24 24" fill="none" aria-hidden="true">
              <path
                d="M5 12h14m-6-6 6 6-6 6"
                stroke="currentColor"
                stroke-width="2"
                stroke-linecap="round"
                stroke-linejoin="round"
              />
            </svg>
          </a>
        </div>


        <div class="impact-visual reveal delay-1">

          <div class="impact-eyebrow">
            <span>O que você consegue analisar com a calculadora</span>
            <span class="status">Análise personalizada</span>
          </div>


          <div class="impact-list">

            <article class="impact-card">
              <span class="impact-number">01</span>
              <span class="impact-tag">Identificar</span>
              <h3>Oportunidades de produtividade</h3>
              <p>
                Veja quais processos podem estar consumindo mais tempo
                do que deveriam.
              </p>
            </article>


            <article class="impact-card">
              <span class="impact-number">02</span>
              <span class="impact-tag">Reduzir</span>
              <h3>Redução de tarefas manuais</h3>
              <p>
                Estime quanto esforço operacional poderia ser reduzido
                com automações.
              </p>
            </article>


            <article class="impact-card">
              <span class="impact-number">03</span>
              <span class="impact-tag">Mensurar</span>
              <h3>Economia de tempo e recursos</h3>
              <p>
                Tenha uma visão mais clara do impacto potencial da
                automação na rotina do time.
              </p>
            </article>


            <article class="impact-card">
              <span class="impact-number">04</span>
              <span class="impact-tag">Priorizar</span>
              <h3>Recomendações de melhoria</h3>
              <p>
                Receba sugestões práticas para priorizar os processos
                com maior potencial de otimização.
              </p>
            </article>

          </div>

        </div>

      </div>

    </section>

  </main>


  <script>
    document.addEventListener("DOMContentLoaded", () => {
      const revealElements = document.querySelectorAll(".reveal");

      if (!("IntersectionObserver" in window)) {
        revealElements.forEach(el => el.classList.add("is-visible"));
        return;
      }

      const observer = new IntersectionObserver(
        entries => {
          entries.forEach(entry => {
            if (entry.isIntersecting) {
              entry.target.classList.add("is-visible");
              observer.unobserve(entry.target);
            }
          });
        },
        {
          threshold: 0.12,
          rootMargin: "0px 0px -30px 0px"
        }
      );

      revealElements.forEach(el => observer.observe(el));

      const calculatorShell = document.querySelector(".calculator-shell");

      if (calculatorShell) {
        calculatorShell.addEventListener("pointermove", event => {
          const rect = calculatorShell.getBoundingClientRect();

          const x = ((event.clientX - rect.left) / rect.width) * 100;
          const y = ((event.clientY - rect.top) / rect.height) * 100;

          calculatorShell.style.setProperty("--mx", `${x}%`);
          calculatorShell.style.setProperty("--my", `${y}%`);
        });
      }
    });
  </script>

<!-- ==========================================
     COMO FUNCIONA
=========================================== -->

<section class="how-section">

  <div class="how-orb how-orb-1" aria-hidden="true"></div>
  <div class="how-orb how-orb-2" aria-hidden="true"></div>

  <div class="how-container">


    <!-- ==========================================
         ABERTURA DA SEÇÃO
    =========================================== -->

    <div class="how-heading reveal">

      <div class="how-heading-top">

        <span class="how-section-index" aria-hidden="true">
          03
        </span>

        <span class="how-kicker">
          <span class="how-kicker-dot"></span>
          Como funciona
        </span>

      </div>


      <h2>
        Sua estimativa em

        <span class="how-title-accent">
          3 etapas
        </span>
      </h2>


      <p>
        Você informa alguns dados sobre a rotina atual da equipe
        e a calculadora transforma essas informações em uma
        estimativa prática de economia e oportunidades de automação.
      </p>

    </div>



    <!-- ==========================================
         ETAPAS
    =========================================== -->

    <div class="how-steps">


      <!-- ETAPA 01 -->

      <article class="how-step reveal">

        <div class="step-marker">
          <span>
            01
          </span>
        </div>

        <div class="step-content">

          <span class="step-label">
            Sua rotina
          </span>

          <h3>
            Informe sua rotina atual
          </h3>

          <p>
            Volume de campanhas, frequência de tarefas
            e tempo gasto pela equipe.
          </p>

        </div>

      </article>



      <!-- ETAPA 02 -->

      <article class="how-step reveal delay-1">

        <div class="step-marker">
          <span>
            02
          </span>
        </div>

        <div class="step-content">

          <span class="step-label">
            A estimativa
          </span>

          <h3>
            Veja o potencial de economia
          </h3>

          <p>
            A calculadora estima quanto esforço manual
            pode ser reduzido.
          </p>

        </div>

      </article>



      <!-- ETAPA 03 -->

      <article class="how-step reveal delay-2">

        <div class="step-marker">
          <span>
            03
          </span>
        </div>

        <div class="step-content">

          <span class="step-label">
            Próximos passos
          </span>

          <h3>
            Identifique onde começar
          </h3>

          <p>
            Receba recomendações para priorizar
            oportunidades de automação.
          </p>

        </div>

      </article>


    </div>

  </div>

</section>


<style>

/* ==================================================
   COMO FUNCIONA
================================================== */

.how-section {
  position: relative;

  padding:
    clamp(72px, 8vw, 102px)
    24px
    clamp(82px, 9vw, 112px);

  background:
    radial-gradient(
      circle at 8% 18%,
      rgba(255,79,139,.05),
      transparent 24%
    ),
    radial-gradient(
      circle at 92% 80%,
      rgba(255,122,48,.045),
      transparent 24%
    ),
    linear-gradient(
      180deg,
      #ffffff 0%,
      #fffaf8 52%,
      #ffffff 100%
    );

  overflow: hidden;
}


/* linha sutil no topo */

.how-section::before {
  content: "";

  position: absolute;

  top: 0;
  left: 50%;

  width:
    min(
      1180px,
      calc(100% - 48px)
    );

  height: 1px;

  transform:
    translateX(-50%);

  background:
    linear-gradient(
      90deg,
      transparent,
      rgba(25,25,35,.07),
      transparent
    );
}


/* ==================================================
   ORBS
================================================== */

.how-orb {
  position: absolute;

  border-radius: 50%;

  pointer-events: none;
}


.how-orb-1 {
  width: 420px;
  height: 420px;

  left: -250px;
  top: 20%;

  background:
    radial-gradient(
      circle,
      rgba(255,79,139,.075),
      transparent 68%
    );

  animation:
    howFloatLeft
    9s
    ease-in-out
    infinite alternate;
}


.how-orb-2 {
  width: 360px;
  height: 360px;

  right: -220px;
  bottom: -120px;

  background:
    radial-gradient(
      circle,
      rgba(255,122,48,.07),
      transparent 68%
    );

  animation:
    howFloatRight
    11s
    ease-in-out
    infinite alternate;
}


@keyframes howFloatLeft {

  to {
    transform:
      translate(
        28px,
        -20px
      );
  }

}


@keyframes howFloatRight {

  to {
    transform:
      translate(
        -26px,
        18px
      );
  }

}


/* ==================================================
   CONTAINER
================================================== */

.how-container {
  position: relative;

  z-index: 2;

  max-width: 1180px;

  margin: 0 auto;

  display: grid;

  grid-template-columns:
    minmax(0, .84fr)
    minmax(480px, 1.16fr);

  gap:
    clamp(
      66px,
      8vw,
      110px
    );

  align-items: start;
}


/* ==================================================
   HEADING
================================================== */

.how-heading {
  position: sticky;

  top: 112px;

  max-width: 535px;
}


/* topo do heading */

.how-heading-top {
  position: relative;

  min-height: 64px;

  margin-bottom: 22px;

  display: flex;

  align-items: center;
}


/* número de seção */

.how-section-index {
  position: absolute;

  left: -10px;
  top: -36px;

  font-size:
    clamp(
      5.5rem,
      8vw,
      8.5rem
    );

  font-weight: 800;

  line-height: 1;

  letter-spacing: -.08em;

  color:
    rgba(25,25,35,.035);

  pointer-events: none;
}


/* COMO FUNCIONA */

.how-kicker {
  position: relative;

  z-index: 2;

  display: inline-flex;

  align-items: center;

  gap: 9px;

  padding:
    9px
    13px;

  border:
    1px solid
    rgba(255,79,139,.12);

  border-radius: 999px;

  background:
    rgba(255,255,255,.8);

  box-shadow:
    0 10px 26px
    rgba(25,25,35,.045);

  backdrop-filter:
    blur(12px);

  color: #555562;

  font-size: .72rem;

  font-weight: 800;

  letter-spacing: .08em;

  text-transform: uppercase;
}


.how-kicker-dot {
  position: relative;

  width: 7px;
  height: 7px;

  border-radius: 50%;

  background:
    linear-gradient(
      135deg,
      #ff4f8b,
      #ff7a30
    );
}


.how-kicker-dot::after {
  content: "";

  position: absolute;

  inset: -5px;

  border:
    1px solid
    rgba(255,79,139,.16);

  border-radius: 50%;

  animation:
    howKickerPulse
    2.6s
    ease-out
    infinite;
}


@keyframes howKickerPulse {

  0% {
    transform: scale(.7);
    opacity: 0;
  }

  30% {
    opacity: 1;
  }

  100% {
    transform: scale(1.5);
    opacity: 0;
  }

}


/* ==================================================
   TITLE
================================================== */

.how-heading h2 {
  margin: 0;

  max-width: 540px;

  color: #191923;

  font-size:
    clamp(
      3rem,
      4.8vw,
      4.8rem
    );

  line-height: .97;

  letter-spacing: -.067em;
}


.how-title-accent {
  position: relative;

  display: inline-block;

  margin-top: 3px;

  background:
    linear-gradient(
      115deg,
      #ff4f8b 0%,
      #ff7a30 100%
    );

  -webkit-background-clip: text;
  background-clip: text;

  color: transparent;
}


/* underline do destaque */

.how-title-accent::after {
  content: "";

  position: absolute;

  left: 4px;
  right: 0;
  bottom: -8px;

  height: 5px;

  border-radius: 999px;

  background:
    linear-gradient(
      90deg,
      rgba(255,79,139,.18),
      rgba(255,122,48,.06)
    );

  transform:
    rotate(-1deg);
}


/* descrição */

.how-heading > p {
  margin:
    31px
    0
    0;

  max-width: 475px;

  color: #5a5a68;

  font-size: 1.02rem;

  line-height: 1.72;
}


/* ==================================================
   STEPS
================================================== */

.how-steps {
  position: relative;

  display: flex;

  flex-direction: column;
}


/* linha vertical */

.how-steps::before {
  content: "";

  position: absolute;

  top: 32px;
  bottom: 32px;

  left: 30px;

  width: 2px;

  background:
    linear-gradient(
      180deg,
      rgba(255,79,139,.12),
      #ff4f8b 28%,
      #ff7a30 72%,
      rgba(255,122,48,.10)
    );

  border-radius: 999px;
}


/* luz percorrendo a linha */

.how-steps::after {
  content: "";

  position: absolute;

  left: 28px;
  top: 36px;

  width: 6px;
  height: 42px;

  border-radius: 999px;

  background:
    linear-gradient(
      180deg,
      rgba(255,255,255,0),
      rgba(255,255,255,.95),
      rgba(255,255,255,0)
    );

  box-shadow:
    0 0 18px
    rgba(255,79,139,.65);

  animation:
    stepLineMove
    5.5s
    ease-in-out
    infinite;
}


@keyframes stepLineMove {

  0% {
    top: 5%;
    opacity: 0;
  }

  15% {
    opacity: 1;
  }

  85% {
    opacity: 1;
  }

  100% {
    top: 88%;
    opacity: 0;
  }

}


/* ==================================================
   STEP
================================================== */

.how-step {
  position: relative;

  display: grid;

  grid-template-columns:
    62px
    1fr;

  gap: 28px;

  padding:
    14px
    0
    38px;
}


.how-step:last-child {
  padding-bottom: 0;
}


/* ==================================================
   MARKER
================================================== */

.step-marker {
  position: relative;

  z-index: 2;

  width: 62px;
  height: 62px;

  display: flex;

  align-items: center;
  justify-content: center;

  border:
    1px solid
    rgba(255,79,139,.13);

  border-radius: 50%;

  background:
    rgba(255,255,255,.96);

  box-shadow:
    0 14px 34px
    rgba(25,25,35,.08);

  transition:
    transform .3s ease,
    box-shadow .3s ease,
    border-color .3s ease;
}


.step-marker span {
  font-size: .74rem;

  font-weight: 800;

  color: #ff4f8b;
}


.how-step:hover .step-marker {
  transform:
    scale(1.07);

  border-color:
    rgba(255,79,139,.25);

  box-shadow:
    0 18px 40px
    rgba(255,79,139,.14);
}


/* ==================================================
   STEP CONTENT
================================================== */

.step-content {
  position: relative;

  padding:
    4px
    0
    34px;

  border-bottom:
    1px solid
    rgba(25,25,35,.075);
}


.how-step:last-child .step-content {
  border-bottom: 0;
}


.step-label {
  display: inline-block;

  margin-bottom: 9px;

  color: #ff648f;

  font-size: .68rem;

  font-weight: 800;

  letter-spacing: .11em;

  text-transform: uppercase;
}


.step-content h3 {
  margin: 0;

  max-width: 500px;

  color: #191923;

  font-size:
    clamp(
      1.45rem,
      2vw,
      1.8rem
    );

  line-height: 1.2;

  letter-spacing: -.035em;
}


.step-content p {
  margin:
    11px
    0
    0;

  max-width: 480px;

  color: #626270;

  font-size: .96rem;

  line-height: 1.65;
}


/* underline hover */

.step-content::after {
  content: "";

  position: absolute;

  left: 0;
  bottom: -1px;

  width: 0;
  height: 1px;

  background:
    linear-gradient(
      90deg,
      #ff4f8b,
      #ff7a30
    );

  transition:
    width .4s
    cubic-bezier(.2,.8,.2,1);
}


.how-step:hover .step-content::after {
  width: 100%;
}


/* ==================================================
   TABLET
================================================== */

@media (max-width: 1000px) {

  .how-container {
    grid-template-columns: 1fr;

    gap: 52px;
  }


  .how-heading {
    position: relative;

    top: auto;

    max-width: 720px;
  }


  .how-heading h2 {
    max-width: 680px;
  }


  .how-heading > p {
    max-width: 620px;
  }


  .how-steps {
    max-width: 720px;
  }

}


/* ==================================================
   MOBILE
================================================== */

@media (max-width: 680px) {

  .how-section {
    padding:
      68px
      20px
      82px;
  }


  .how-container {
    gap: 42px;
  }


  .how-heading-top {
    min-height: 54px;

    margin-bottom: 18px;
  }


  .how-section-index {
    top: -26px;

    font-size: 5.6rem;
  }


  .how-kicker {
    padding:
      8px
      11px;

    font-size: .67rem;
  }


  .how-heading h2 {
    font-size:
      clamp(
        2.7rem,
        12vw,
        3.6rem
      );
  }


  .how-heading > p {
    margin-top: 28px;

    font-size: 1rem;
  }


  .how-step {
    grid-template-columns:
      52px
      1fr;

    gap: 18px;

    padding-bottom: 30px;
  }


  .how-steps::before {
    left: 25px;
  }


  .how-steps::after {
    left: 23px;
  }


  .step-marker {
    width: 52px;
    height: 52px;
  }


  .step-content {
    padding-bottom: 27px;
  }


  .step-content h3 {
    font-size: 1.35rem;
  }


  .step-content p {
    font-size: .93rem;
  }

}


/* ==================================================
   REDUCED MOTION
================================================== */

@media (prefers-reduced-motion: reduce) {

  .how-orb,
  .how-kicker-dot::after,
  .how-steps::after {
    animation: none;
  }

}

</style>
